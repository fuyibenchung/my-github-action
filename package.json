{
    "name": "my-github-action",
    "version": "1.0.0",
    "description": "A custom GitHub Action",
    "main": "dist/main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "webpack --config webpack.config.js && zip dist dist.zip"
    },
    "keywords": [
        "github-actions"
    ],
    "author": "Your Name",
    "license": "MIT",
    "dependencies": {
        "@actions/core": "^1.10.1"
    },
    "devDependencies": {
        "webpack": "^5.91.0",
        "webpack-cli": "^5.1.4",
        "zip-dir": "^2.0.0"
    }
}
