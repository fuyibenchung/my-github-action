name: 'My GitHub Action'
description: 'A custom GitHub Action to perform a specific task'
inputs:
  name:
    description: 'An input parameter for the action'
    required: true
runs:
  using: 'node16'
  main: 'dist/bundle.js'